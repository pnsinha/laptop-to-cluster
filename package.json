{
  "name": "bssw-fellowship-resource-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.15.0"
  },
  "scripts": {
    "build": "astro build --root site",
    "check": "astro check --root site",
    "test": "npm run test:unit",
    "test:unit": "vitest run --dir tests/unit",
    "test:property": "vitest run --config vitest.property.config.ts",
    "test:integration": "npm run build && npm run test:integration:built",
    "test:integration:built": "vitest run --config vitest.integration.config.ts",
    "test:accessibility": "npm run build && npm run test:accessibility:built",
    "test:accessibility:built": "vitest run --dir tests/accessibility",
    "test:accessibility:browser": "tsx scripts/scan-accessibility.ts",
    "test:accessibility:coverage": "npm run build && npm run test:accessibility:built && npm run test:accessibility:browser",
    "record-run": "tsx scripts/record-run.ts",
    "validate:evidence": "tsx scripts/validate-evidence.ts",
    "validate:repository": "tsx scripts/validate-repository.ts",
    "validate:release": "tsx scripts/validate-release.ts",
    "validate:built": "node scripts/publication.mjs built-output site/dist",
    "validate:policy": "node scripts/publication.mjs policy .",
    "validate:accessibility-review": "tsx scripts/validate-accessibility-review.ts releases/v0.1.0/accessibility-review.yml",
    "publication:prepare": "node scripts/prepare-publication.mjs .",
    "publication:validate": "npm run validate:built && npm run validate:policy && npm run validate:accessibility-review && npm run validate:evidence && npm run validate:repository && npm run validate:release",
    "validate": "npm run check && npm run test:unit && npm run test:property && npm run build && npm run test:integration:built && npm run test:accessibility:built && npm run test:accessibility:browser && npm run publication:validate && npm run publication:prepare"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "@axe-core/playwright": "4.11.0",
    "@playwright/test": "1.56.1",
    "@types/node": "22.15.3",
    "fast-check": "4.9.0",
    "picomatch": "4.0.5",
    "tsx": "4.23.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10",
    "yaml": "2.9.0"
  }
}
