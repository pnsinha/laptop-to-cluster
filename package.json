{
  "name": "bssw-fellowship-resource-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.15.0" },
  "scripts": {
    "build": "astro build --root site",
    "check": "astro check --root site",
    "test": "npm run test:unit",
    "test:unit": "vitest run --dir tests/unit",
    "test:property": "vitest run --dir tests/property --passWithNoTests",
    "test:integration": "npm run build && vitest run --dir tests/integration",
    "test:accessibility": "npm run build && vitest run --dir tests/accessibility",
    "validate:repository": "tsx scripts/validate-repository.ts",
    "validate:release": "npm run build && tsx scripts/validate-release.ts",
    "validate": "npm run check && npm run test:unit && npm run test:property && npm run test:integration && npm run test:accessibility && npm run validate:repository && npm run validate:release"
  },
  "dependencies": {
    "astro": "7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "@types/node": "22.15.3",
    "fast-check": "4.9.0",
    "picomatch": "4.0.5",
    "tsx": "4.23.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10",
    "yaml": "2.9.0"
  }
}
