# Baseline Slurm + Apptainer workflow

This directory reserves the designed public path for the Milestone 1 reference workflow. The runnable implementation is delivered by the dedicated baseline-workflow task; this foundation intentionally makes no unvalidated execution claim.

Planned stable subdirectories are `bin/`, `slurm/`, `container/`, `inputs/`, `expected/`, and `tests/`.
